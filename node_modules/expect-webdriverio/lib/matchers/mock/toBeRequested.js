import { toBeRequestedTimes } from './toBeRequestedTimes.js';
export function toBeRequested(received, options = {}) {
    return toBeRequestedTimes.call({ ...(this || {}), expectation: 'called' }, received, { ...options, gte: 1 });
}
