import { toHaveUrl } from './toHaveUrl.js';
export function toHaveUrlContaining(browser, url, options = {}) {
    return toHaveUrl.call(this, browser, url, {
        ...options,
        containing: true
    });
}
