import { toHaveTitle } from './toHaveTitle.js';
export function toHaveTitleContaining(browser, title, options = {}) {
    return toHaveTitle.call(this, browser, title, {
        ...options,
        containing: true
    });
}
