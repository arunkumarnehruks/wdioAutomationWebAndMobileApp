import { executeCommandBe } from '../../utils.js';
export function toBeFocused(received, options = {}) {
    this.expectation = this.expectation || 'focused';
    return executeCommandBe.call(this, received, el => el.isFocused(), options);
}
