import { toHaveAttributeAndValueFn } from './toHaveAttribute.js';
export function toHaveHref(el, href, options = {}) {
    return toHaveAttributeAndValueFn.call(this, el, 'href', href, options);
}
export const toHaveLink = toHaveHref;
