import { toHaveHref } from './toHaveHref.js';
export function toHaveHrefContaining(el, href, options = {}) {
    return toHaveHref.call(this, el, href, {
        ...options,
        containing: true
    });
}
export const toHaveLinkContaining = toHaveHrefContaining;
