import { toHaveText } from './toHaveText.js';
export function toHaveTextContaining(el, text, options = {}) {
    return toHaveText.call(this, el, text, {
        ...options,
        containing: true
    });
}
