import { executeCommandBe } from '../../utils.js';
export function toBeClickable(received, options = {}) {
    this.expectation = this.expectation || 'clickable';
    return executeCommandBe.call(this, received, async (el) => {
        try {
            return el.isClickable();
        }
        catch {
            return false;
        }
    }, options);
}
