import { toHaveValue } from './toHaveValue.js';
export function toHaveValueContaining(el, value, options = {}) {
    return toHaveValue.call(this, el, value, {
        ...options,
        containing: true
    });
}
