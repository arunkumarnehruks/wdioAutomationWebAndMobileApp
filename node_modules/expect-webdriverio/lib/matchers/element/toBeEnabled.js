import { executeCommandBe } from '../../utils.js';
export function toBeEnabled(received, options = {}) {
    this.expectation = this.expectation || 'enabled';
    return executeCommandBe.call(this, received, el => el.isEnabled(), options);
}
